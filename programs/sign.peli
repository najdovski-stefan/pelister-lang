( Determine the sign of a number (n) on the stack. )
( Replaces n with -1, 0, or 1. )

( --- USAGE --- )
(examples: 99, -42, or 0 )

-420.0

DUP 0 =
IF
    DROP 0
ELSE
    DUP 0 >
    IF
        DROP 1
    ELSE
        DROP -1
    THEN
THEN

CR .
